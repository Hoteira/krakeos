[package]
name = "libc"
version = "0.1.0"
edition = "2024"

[dependencies]
std = { path = "../../std", default-features = false }

[features]
default = ["userland"]
userland = ["std/userland", "std/global_allocator"]

[lib]
crate-type = ["staticlib", "rlib"]
