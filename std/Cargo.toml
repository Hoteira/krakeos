[package]
name = "std"
version.workspace = true
authors.workspace = true
edition.workspace = true

[lib]
crate-type = ["rlib"]

[features]
default = ["userland", "global_allocator"]
userland = []
global_allocator = []

[dependencies]
# common = { path = "../common" }
